// extracted from NeonSerpentGame_backup.jsx on 2025-04-17
// TODO: Move drawWorld logic here: background, orbs, particles, snakes, HUD, overlays

/**
 * Draw the entire game world to canvas context
 * @param {object} world - current world state
 * @param {CanvasRenderingContext2D} ctx - canvas context
 * @param {object} view - view dimensions {w, h}
 * @param {HTMLImageElement} bgImg - background image
 * @param {boolean} bgReady - whether background image loaded
 */
export function drawWorld(world, ctx, view, bgImg, bgReady) {
  // TODO: copy implementation from backup
}
