// extracted from NeonSerpentGame_backup.jsx on 2025-04-17
import React from 'react';

/**
 * Controls overlay component
 * @param {{ onBack: Function }} props
 */
export default function ControlsOverlay({ onBack }) {
  // TODO: move controls overlay JSX here from backup file
  return (
    <div className="controls-overlay">
      {/* ...existing code... */}
      <button onClick={onBack}>Back</button>
    </div>
  );
}
